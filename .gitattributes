# Normalize text line endings in the repo; checkout native on each platform
* text=auto

# Binary / specially-encoded assets — never apply EOL or encoding conversion
*.dll binary
*.exe binary
*.ans binary
letters.xml binary
